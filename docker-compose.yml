services:
  delist_scraper:
    image: python:3.12-slim
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "bash install.sh --nosudo && bash run.sh"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
